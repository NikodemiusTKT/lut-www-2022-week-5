var express = require('express');
var router = express.Router();
/* GET home page. */
router.post('/', (req, res ) => {
  res.send('This is un-implemented image POST route.')
});


module.exports = router;
